# xee-span

Source code in Xee can has an origin (file, inline) and a span.

Here we centralize the whole span story. Depending on the usage profile you can
enable chumsky (parser) and ariadne (error display) features.

